@model UserInfo
@{
ViewBag.Title = "Başvuru Durumu";
}

<main class="text-center">
    <h1 class="h4">@ViewBag.Title</h1>
    @if (Model.WillAttend)
    {
        <p>Teşekkürler,@Model.Name toplantıya sizinle birlikte @ViewBag.UserCount kişi katılıyor.</p>
        <a class="btn btn-sm btn-outline-primary" href="/meeting/list">Katılımcı Listele</a>
    }
    else 
    {
        <p>@Model.Name, toplantıya katılmadığından dolayı üzgünüz.</p>
        <a class="btn btn-sm btn-outline-primary" href="/meeting/apply">Başvuru Formu</a>
    }
</main>